spring: 
  application: 
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          searchPaths: respo
          username: your username
          password: your password
      label: master
server:
  port: 8888


